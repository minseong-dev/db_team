exports.recentGameList = 'select a.game_location, a.game_date, b.team_name, b.score, c.team_name, c.score from game a, team_game b, team_game c where a.game_num = b.game_num and a.game_num = c.game_num and a.league_num = b.league_num and a.league_num = c.league_num and b.team_name != c.team_name order by a.game_date desc, a.game_num'